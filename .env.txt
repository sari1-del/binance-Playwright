# The bot scans USD-M Futures top 24-hour gainers automatically.
FUTURES_API_BASE=https://fapi.binance.com
MIN_QUOTE_VOLUME=20000000
NO_REPEAT_SIGNALS=5
# Mount a Railway Volume at /data so this state survives restarts.
SIGNAL_STATE_PATH=/data/recent-signals.json

# Binance Futures chart URL; {symbol} is filled automatically.
CHART_URL_TEMPLATE=https://www.binance.com/en/futures/{symbol}
CHART_SELECTOR=#tv_chart_container

STORAGE_STATE_B64=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Every 20 minutes.
CRON_SCHEDULE=*/20 * * * *

# Keep this true until the generated post and Square selectors are verified.
DRY_RUN=true
