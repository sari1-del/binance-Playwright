# Coins to cover, comma-separated
SYMBOLS=BTCUSDT,ETHUSDT

# Chart page template — {symbol} gets replaced per coin
CHART_URL_TEMPLATE=https://www.binance.com/en/trade/{symbol}?type=spot

# CSS selector for the chart element to screenshot (verify with codegen)
CHART_SELECTOR=#tv_chart_container

# Output of `npm run login`, base64-encoded Playwright storage state
STORAGE_STATE_B64=

# OpenRouter
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o

# Cron schedule for how often to run a full cycle (default: every 4 hours)
CRON_SCHEDULE=0 */4 * * *

# true = log the post instead of publishing it. Flip to false only once
# you've verified selectors and output look right.
DRY_RUN=true
